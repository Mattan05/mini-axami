<form method="post" action="{{ path('app_licensekeys_delete', {'id': licensekey.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ licensekey.id) }}">
    <button class="btn">Delete</button>
</form>
